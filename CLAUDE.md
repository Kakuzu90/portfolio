# CLAUDE.md

Follow `AGENTS.md` as the primary project guide.

## Working Notes

- Framework: Vue 3 with Vite and Vue Router.
- Content source: `data.json`.
- Routes: `/`, `/dev`, and `/client`.
- Theme preference key: `theme` with `light` or `dark`.
- Portfolio preference key: `portfolio_view` with `dev` or `client`.
- Palette preference key: `portfolio_palette` with `cobalt`, `forest`, `aubergine`, `cyan`, or `lime`.
- Keep the existing minimal red, white, and dark visual identity.
- Prefer data-driven Vue components over hard-coded repeated markup.
- Preserve direct-route behavior, responsive layouts, accessibility, print styles, and reduced-motion support.
- Validate changes with `npm run build`.
